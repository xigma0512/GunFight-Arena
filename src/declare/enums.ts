export enum Status {
    Waiting,
    Preparing,
    Running,
    GameOver
}

export enum EGameMode {
    DeathMatch,
    Demolition // 爆破模式
}

export enum Team {
    None,
    Red,
    Blue,
    Spectator,
}