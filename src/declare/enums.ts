export enum Status {
    Waiting,
    Preparing,
    Running,
    GameOver
}

export enum GameMode {
    DeathMatch,
    Demolition // 爆破模式
}

export enum Team {
    None,
    Red,
    Blue,
    Spectator,
}